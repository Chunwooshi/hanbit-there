<li>
    <div class="header-menu-text">
        {{~name~}}
    </div>
    <ul class="h-sub-m">
        {{#each subMenus ~}}
            <li theres-id="{{id}}">{{~name~}}</li>
        {{~/each}}
    </ul>
</li>